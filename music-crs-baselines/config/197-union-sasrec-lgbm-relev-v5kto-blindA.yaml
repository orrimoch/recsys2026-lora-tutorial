# 197 — Union (BM25 + instruct-dense + same-artist + SASRec) recall -> LGBM rerank
#        lgbm_relev (Tier-2 #4.1 relevance feats qwen_meta_cos + bm25_score, #4.3
#        5-seed bagging, leak-free temporal-holdout early-stop) -> v5-kto responder.
#        Blind-A submission. Clone of config 194; ONLY reranker_model_path differs.
#
# DEV nDCG@20 (nb74 cell 12b, goal-ful query == this serve config):
#   recall-only            = 0.1498
#   lgbm_clean_full (194)  = 0.1623  (turn1 0.1644)
#   lgbm_relev   (THIS)    = 0.1652  (turn1 0.1819)   <- beats 194 + the 2k model
#
# The relevance features auto-inject at serve: crs_baseline computes qwen_meta_cos
# + bm25_score whenever the loaded reranker lists them (shared RelevanceScorer ->
# train/serve parity). No other config change vs 194.

lm_type: "OrRim123/recsys2026-b3-grpo-pilot-2026-05-13-qwen3b-v5-kto-merged"
lora_path: null
lora_max_rank: 32

retrieval_type: "wrrf_union_v1"

use_sasrec: true
w_sasrec: 1.0
sasrec_model_dir: "sasrec_v1"

test_dataset_name: "talkpl-ai/TalkPlayData-Challenge-Blind-A"
item_db_name: "talkpl-ai/TalkPlayData-Challenge-Track-Metadata"
user_db_name: "talkpl-ai/TalkPlayData-Challenge-User-Metadata"
track_split_types:
  - "all_tracks"
user_split_types:
  - "all_users"
corpus_types:
  - "track_name"
  - "artist_name"
  - "album_name"
cache_dir: "../experiments/cache"
device: "cuda"
attn_implementation: "sdpa"

retrieval_topk: 100
reranker_type: "lgbm_rerank"
reranker_model_path: "/content/drive/MyDrive/recsys2026_retrieval_v2_cache/lgbm/lgbm_relev"

response_prompt_name: "response_generation_cot_user_state"
response_max_new_tokens: 320
top_n_for_prompt: 1
query_preprocessing_mode: "raw_with_goal"

use_vllm: false

use_state_tracker: true
state_tracker_prompt_name: "state_extraction"
state_tracker_max_new_tokens: 96

use_cmqr: false
