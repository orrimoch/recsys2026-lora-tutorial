# Wave 2 — first two-step experiment on fresh-model branch.
# Retrieval: wRRF(BM25 + dense-metadata + dense-lyrics) — best retrieval-only config from Wave 1 (nDCG@20=0.0994).
# Response: Qwen2.5-1.5B-Instruct (already-proven MPS-fp32-safe path from memory).
# Expected composite_retrieval ≈ 0.10–0.12 if LexDiv lands near B-floor's 0.25.
# Shipping target: > B-floor composite_retrieval (0.1042) → first internal champion.

lm_type: "Qwen/Qwen2.5-1.5B-Instruct"
retrieval_type: "wrrf_bm25_dense_lyrics_v1"
test_dataset_name: "talkpl-ai/TalkPlayData-Challenge-Dataset"
item_db_name: "talkpl-ai/TalkPlayData-Challenge-Track-Metadata"
user_db_name: "talkpl-ai/TalkPlayData-Challenge-User-Metadata"
track_split_types:
  - "all_tracks"
user_split_types:
  - "all_users"
corpus_types:
  - "track_name"
  - "artist_name"
  - "album_name"
cache_dir: "../experiments/cache"
device: "mps"
attn_implementation: "eager"
